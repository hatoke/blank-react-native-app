import { StyleSheet } from "react-native";

//TODO verilen renkerin tonlarını yapacak bir şey işte
const create_dark_light_color = (color_name, color_hex) => {

};

const style = StyleSheet.create({});

export default style;
